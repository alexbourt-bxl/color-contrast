name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

  build-macos-picker:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Build macOS pixel picker helper
        run: chmod +x scripts/build-mac.sh && scripts/build-mac.sh

      - name: Commit updated mac helper binary
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git add assets/mac

          if [ -z "$(git status --porcelain -- assets/mac)" ]; then
            echo "assets/mac unchanged; skipping commit."
            exit 0
          fi

          git -c user.name="github-actions[bot]" -c user.email="github-actions[bot]@users.noreply.github.com" \
            commit -m "chore: update mac helper binary"

          git pull --rebase
          git push

      - name: Upload macOS pixel picker helper
        uses: actions/upload-artifact@v4
        with:
          name: mac
          path: assets/mac
          if-no-files-found: error
